<%= form_with id: "todo-form",
        model: todo,
        data: {
          action: "submit->todo#create",          
        } do |f|
  %>
    <%= f.text_field :title, data: {target: 'todo.focus'} %>
    <%= f.submit "Add todo" %>
  <% end %>